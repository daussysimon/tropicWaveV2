import React from "react";
import "./style.scss";

import NavBar from "./Navbar";

const Header = ({ title, subtitle, backgroundImage }) => {
  return <header className="header__container">{/* <NavBar /> */}</header>;
};

export default Header;
